import { ModalDirectiveDirective } from './modal-directive.directive';

describe('ModalDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
